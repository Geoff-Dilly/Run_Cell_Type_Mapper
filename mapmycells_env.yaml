name: mapmycells_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - r-base
  - python=3.11
  - r-seurat
  - h5py=3.0.9
  - r-tidyverse
  - r-here
  - scanpy=1.10.0
  - anndata=0.12.0
  - bioconductor-singlecellexperiment
  - bioconductor-zellkonverter
  - r-biomart

  # Others may need to be installed in R
  # Rscript -e 'install.packages("curl")'
  # Rscript -e 'install.packages("httr")'